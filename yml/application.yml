spring:
  application:
    name: proy-movimientos-service
  datasource:
    url: jdbc:mysql://localhost:3302/eurekabank
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root123
  freemarker:
    enabled: false
  thymeleaf:
    cache: false
    enabled: false
  mvc:
    view:
      prefix: /WEB-INF/view/
      suffix: .jsp
 
spring:
  application:
    name: proy-categoria-service
  mvc:
    view:
      prefix: /WEB-INF/view/
      suffix: .html
  freemarker:
    enabled: false
  thymeleaf:
    enabled: false
    cache: false
    prefix: classpath:/templates/
  datasource:
    tienda:
      url: jdbc:mysql://localhost:3302/tienda
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: root123 
 
 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka